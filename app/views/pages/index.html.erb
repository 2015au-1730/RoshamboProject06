<div align="center">
  <h2>Editor's selection for you!</h2>
  <table border="0">
    <% @list.each do |l| %>
        <tr>
          <th style="text-align: left;"><%= l.cost %></th>
          <th/>
          <th/>
          <th><a href="<%= l.link %>"><%= l.description %></a></th>
          <th style="display:none;"><%= l.category %></th>
        </tr>
    <% end %>
  </table>
</div>